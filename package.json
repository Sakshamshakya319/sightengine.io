{
  "name": "socio-io",
  "version": "1.0.0",
  "description": "A content filtering extension that uses AI to detect and filter inappropriate content",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "build": "node backend/build.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.6.8",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2"
  }
}